#include <stdio.h>

int main() {
    char ch;
    
    printf("Enter the first character: ");
    scanf(" %c", &ch);
    switch (ch){
    case 1:
    if (ch == 'a' || ch == 'e' || ch == 'i' || ch == 'o' || ch == 'u' ||
        ch == 'A' || ch == 'E' || ch == 'I' || ch == 'O' || ch == 'U') {
        printf("The alphabet is present in the vowel\n");
    } else {
        printf("The alphabet is not present in the vowel\n");
    }
    }
    case 2
    return 0;
}
